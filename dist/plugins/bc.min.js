/*export default class BC extends HTML{
	constructor(bc_php = "../../src/plugins/BC/loadBC.php"){
		super();
		this._bc_php = bc_php;
		if(!window.jQuery) throw new Error("Jquery needs to be imported first!");
	}
	load(track){
		if(!this.constructor._validTrack(track)) throw new Error("Invalid Filetype");
		if(track.bc_url == track.src){
			//this._player.src = 'data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAVFYAAFRWAAABAAgAZGF0YQAAAAA='; //stop playing silently
			//console.log("Loading bc_url...");
			let self = this;
			let p = this.waitForEvent('loaded');
			new Promise(function(resolve,reject){
				$.ajax({
					url: self._bc_php,
					type: 'POST',
					data: {href:track.bc_url},
					success: function(data) {
						var tracks = JSON.parse(data);
						if(Object.values(tracks).length != 1) return reject("Invalid Url");
						track.src = Object.values(tracks)[0];
						self.load(track).then(resolve,reject);
					},
					error: function(xhr,status,error){
						reject(xhr);
					}
				});
			}).catch(function(error){
				let data = self._publish('error');
			});
			return p;
		}
		return super.load(track);
	}
	
}
BC.Track = class Track extends HTML.Track{
	constructor(obj){
		super(obj);
		this.filetype = "BC";
		this.bc_url = this.src; //copy for later
	}
	toJSON(){
		let obj = super.toJSON();
		obj.src = this.bc_url;
		return obj;
	}
}
BC.Track.fromJSON = function(json){
	return new BC.Track(JSON.parse(json));
}
BC._validURL = function(url){
	try{
		let tmp = new URL(url);
		let arr = tmp.hostname.split('.');
		arr.shift();
		arr = arr.join('.');
		if(arr == "bandcamp.com") return true;
		return false;
	}catch(e){
		return false;
	}
}*/
import HTML from '../html.min.js';
export default class BC extends HTML{constructor(r="../../dist/bc.min.php"){if(super(),this._bc_php=r,!window.jQuery)throw new Error("Jquery needs to be imported first!")}load(r){if(!this.constructor._validTrack(r))throw new Error("Invalid Filetype");if(r.bc_url==r.src){let t=this,e=this.waitForEvent("loaded");return new Promise(function(e,s){$.ajax({url:t._bc_php,type:"POST",data:{href:r.bc_url},success:function(c){var n=JSON.parse(c);if(1!=Object.values(n).length)return s("Invalid Url");r.src=Object.values(n)[0],t.load(r).then(e,s)},error:function(r,t,e){s(r)}})}).catch(function(r){t._publish("error")}),e}return super.load(r)}}BC.Track=class extends HTML.Track{constructor(r){super(r),this.filetype="BC",this.bc_url=this.src}toJSON(){let r=super.toJSON();return r.src=this.bc_url,r}},BC.Track.fromJSON=function(r){return new BC.Track(JSON.parse(r))},BC._validURL=function(r){try{let t=new URL(r).hostname.split(".");return t.shift(),"bandcamp.com"==(t=t.join("."))}catch(r){return!1}};
